# 📅 获取日程列表接口 (万无一失版)

此接口用于获取指定日期的所有日程。前端在日历视图切换日期时调用。

## 1. 接口定义

*   **接口地址**: `/api/schedules`
*   **请求方法**: `GET`
*   **认证方式**: Header 中必须携带 `Authorization: Bearer {token}`

## 2. 请求参数 (Query)

| 参数名 | 类型 | 必填 | 说明 | 示例 |
| :--- | :--- | :--- | :--- | :--- |
| **`date`** | String | **是** | 目标日期，格式必须为 `YYYY-MM-DD` | `2026-01-14` |

> **⚠️ 特别注意**: 请务必确保 `date` 参数的年份正确！如果用户在看 2025 年的日历，不要传成 2026 年。

**请求示例**:
`GET /api/schedules?date=2026-01-14`

## 3. 响应结构 (JSON)

后端返回一个标准的 `ApiResponse` 结构，实际的日程列表在 `data` 字段中。

```json
{
  "code": 200,
  "message": "获取成功",
  "data": [
    // 日程对象列表，如果没有日程则为空数组 []
    {
      "scheduleId": "f15197d2-e760-4e9b-b173-5e3b2348696d",
      "title": "项目周会",
      "scheduleDate": "2026-01-14",
      "startTime": "14:30:00",
      "endTime": "15:30:00",
      "location": "301会议室",
      "isAllDay": false,
      "category": "工作",
      "isAiGenerated": false,
      "color": "#4AC4CF",
      "notes": "带上笔记本",
      "reminderMinutes": 15,
      "repeatType": "none",
      "userId": "user_123",
      "createdAt": 1705234567890
    }
  ]
}
```

## 4. 字段详细对照表 (前端必读)

前端在解析 `data` 数组中的对象时，请严格对照下表：

| 字段名 | 类型 | 可能为 null | 说明 | 示例值 |
| :--- | :--- | :--- | :--- | :--- |
| **`scheduleId`** | String | 否 | 日程唯一ID (主键) | `"sch_123"` |
| **`title`** | String | 否 | 日程标题 | `"开会"` |
| **`scheduleDate`**| String | 否 | 日期 (YYYY-MM-DD) | `"2026-01-14"` |
| **`startTime`** | String | **是** | 开始时间 (HH:mm:ss 或 HH:mm) <br> *注: 全天日程可能为 null* | `"14:30:00"` |
| **`endTime`** | String | **是** | 结束时间 (HH:mm:ss 或 HH:mm) | `"15:30:00"` |
| **`isAllDay`** | Boolean| 否 | 是否全天 (`true`/`false`) | `false` |
| **`location`** | String | **是** | 地点 | `"会议室"` |
| **`category`** | String | **是** | 分类 (工作/生活/其他...) | `"工作"` |
| **`isAiGenerated`**| Boolean| 否 | 是否AI生成 (`true`/`false`) | `false` |
| **`color`** | String | **是** | 颜色 (Hex) | `"#FF5722"` |
| **`notes`** | String | **是** | 备注 | `"记得带水"` |
| **`reminderMinutes`**| Integer| **是** | 提前提醒分钟数 | `15` |
| **`repeatType`** | String | **是** | 重复类型 (none/daily/weekly...) | `"none"` |

## 5. 常见问题排查 (FAQ)

**Q1: 为什么添加成功了，但获取列表返回空数组 `[]`？**
*   **检查年份**: 请确认你添加日程时的年份（例如 2026）和查询时的 `date` 参数年份（例如 2025）是否一致。
*   **检查日期格式**: `date` 参数必须是 `YYYY-MM-DD`，例如 `2026-01-05`（注意个位数要补0）。

**Q2: `startTime` 有时候带秒，有时候不带？**
*   后端返回的是标准时间字符串，通常是 `HH:mm:ss` (如 `14:30:00`)。前端展示时建议截取前5位 (`14:30`)。

**Q3: `id` 字段去哪了？**
*   后端返回的主键字段名是 **`scheduleId`**，不是 `id`。请前端适配。
